# Pre-built test image for frontend with node_modules pre-installed
FROM node:18

WORKDIR /app

# Copy package files and install dependencies
# This gets cached in the image
COPY frontend/package*.json ./
RUN npm ci

# The app code will be mounted at runtime

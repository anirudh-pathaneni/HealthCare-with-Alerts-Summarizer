[defaults]
# Look for roles in the roles directory (relative to ansible.cfg location)
roles_path = ./roles

# Use local inventory (relative path)
inventory = ./inventory/hosts

# Disable host key checking for local execution
host_key_checking = False

# Reduce verbosity of warnings
deprecation_warnings = False

# Auto-detect Python interpreter
interpreter_python = auto_silent
